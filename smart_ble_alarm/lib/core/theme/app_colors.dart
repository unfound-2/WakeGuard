import 'package:flutter/material.dart';

class AppColors {
  // Primary Dark Theme Colors
  static const Color background = Color(0xFF050508); // Ultra deep void black
  static const Color surface = Color(0xFF101018); // Slightly elevated dark
  static const Color surfaceHighlight = Color(0xFF2A2A38); // Subtle border
  
  // Primary Light Theme Colors
  static const Color lightBackground = Color(0xFFF2F2F7); // iOS grouped background
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightSurfaceHighlight = Color(0xFFE5E5EA);
  
  // Accent Colors
  static const Color primaryOrange = Color(0xFFFF5500); // Neon vibrant orange
  static const Color primaryOrangeDim = Color(0x33FF5500);
  
  static const Color neonBlue = Color(0xFF00E5FF); // Secondary futuristic accent
  
  // Text Colors (Dark Mode)
  static const Color textPrimary = Color(0xFFF0F0F5);
  static const Color textSecondary = Color(0xFF808090);
  
  // Text Colors (Light Mode)
  static const Color textPrimaryLight = Color(0xFF000000);
  static const Color textSecondaryLight = Color(0xFF8E8E93);
  
  // Feedback Colors
  static const Color error = Color(0xFFFF2A2A);
  static const Color success = Color(0xFF00FF88);
}
